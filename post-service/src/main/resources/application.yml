datasource:
  url: https://jsonplaceholder.typicode.com/
logging:
  level:
    root: INFO
    com:
      github:
        madhawas1:
          postservice: DEBUG
management:
  endpoints:
    web:
      base-path: /
server:
  port: 3001
  servlet:
    context-path: /api
spring:
  output:
    ansi:
      enabled: always
